package InterfaceExample;

public interface Shape {
    //These next two methods are examples of abstract methods.
    public double getArea();
    public double getPerimeter();

    //Interfaces only have method names and signatures (abstract methods). The implementing class is responsible for
    //implementing the method(s).
}
